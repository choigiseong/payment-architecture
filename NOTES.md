# 설계 노트

> **왜 그렇게 했나**와 **무엇을 안 했나**를 남기는 문서. 무엇을 만들었는지는 [README](README.md)에 있다.
>
> 코드 주석(TODO)과 나누는 기준은 **붙일 코드 줄이 있는가**다. 있으면 TODO로 그 자리에 두고, 없거나 애매하면 여기에 적는다.
> TODO는 그 줄을 고치려는 사람이 반드시 보게 되지만, 여기 있는 것은 아무도 열지 않으면 그대로 잊힌다.
>
> 결정의 근거가 된 실서비스 조사는 [docs/har-analysis.md](docs/har-analysis.md)에 따로 있다.

---

## 1. 결정된 것 (근거 포함)

논의 끝에 확정된 것들. 코드만 봐서는 왜 그런지 알기 어려운 것 위주.

**동기 승인 유지 (A안), 비동기 워커(B안)는 아직 안 함**
무신사·쿠팡도 동기 승인 + 폴링 폴백으로 운영하는 것을 HAR로 확인. B안의 이유는 정확성이 아니다. 이유가 둘인데 성격이 다르다 *(2026-08-22 보강 — 처음엔 부하만 적었다)*:
- **부하** — 요청 스레드가 승인 응답을 기다리며 점유된다. 지금 규모에서는 갈 이유가 없다.
- **가용성** — 규모와 무관한 장애 격리 문제라 "규모가 오면"으로 미룰 수 없다. 아래 «PG가 죽어도 주문은 받는다»가 이쪽이고, B안은 그 수단이다.

부하 쪽 전환 조건은 `BillingPaymentFacade`에 TODO.

**PG가 죽어도 주문은 받는다** *(2026-08-22 목표 — 수단 미정)*
배송의 약속이 "못 지킬 날짜는 팔지 않는다"이면, 가용성의 약속은 이것이다. 같은 층위로 둔다. 한동안 B안 항목의 불릿 하나로 묻혀 있어서 "B안은 안 함"만 읽히고 목표가 보이지 않았다.

지금은 뚫려 있다. 승인이 동기라 요청 스레드를 최대 70초 잡고, 톰캣 기본 풀(200)을 결제·상품·장바구니·결과 폴링이 함께 쓴다. 초당 3건이면 210건에서 마르므로 **Toss 장애가 곧 우리 전체 장애**가 된다. 이미 결제한 사람이 결과 페이지를 못 보는 것이 특히 나쁘다.

"주문을 받는다"가 두 가지 뜻인 것에 주의한다. **(가) 나머지 화면은 산다** — 승인 대기를 톰캣 스레드에서 떼면 얻지만, 그 주문 자체는 승인되지 않는다. **(나) 접수해두고 나중에 승인한다** — 이게 문자 그대로의 목표이고 B안이다.

(나)는 **5분 룰과 부딪힌다.** `prepare()`가 커밋되므로 거래는 PENDING으로 남고, 워커가 승인을 못 쏘면 90초 뒤 조회에 기록이 없어 `Unknown`, 300초에 `NOT_CONFIRMED`로 죽는다. PG가 10분 죽어 있으면 그동안 접수한 주문이 전부 5분 뒤 버려진다. 받아놓고 버리면 받은 것이 아니다.
**보장과도 부딪힌다.** 접수 시점에 "내일 도착"으로 팔았는데 승인이 마감 뒤에 성공하면 못 지킨다. 「보장 위반 처분」이 드문 예외가 아니라 **장애 때마다 대량으로** 일어난다.

수단은 셋 중 하나를 골라야 하고 아직 안 골랐다:

| 안 | 하는 일 | 대가 |
|---|---|---|
| 장애 중 접수는 D+2로 판다 | PG가 죽었으면 내일 도착을 약속하지 않는다. 마감까지 하루 여유가 생겨 5분 룰을 안 건드린다 | "PG 죽음"을 판단할 장치가 필요하다(승인 쪽 서킷). 사용자는 하루 늦은 날짜를 보고 산다 |
| 5분 룰을 마감 절대시각에 연동 | 오전 10시 장애면 마감까지 12시간이라 5분에 죽이지 않는다 | 3장 「우리 DB 장애로 성공한 결제가 취소된다」와 같은 해법이라 한 번에 둘이 풀린다. 대신 기한 규칙 전체를 다시 짠다 |
| 접수만 하고 보장을 안 건다 | "결제 대기" 주문으로 받고 날짜는 확정 후 약속 | 보장 상품인데 날짜 없이 파는 것이 기존 원칙과 충돌한다 |

**배송일은 "예정"이 아니라 "보장"**
못 지킬 날짜는 애초에 팔지 않는다. 그래서 결제 시점에 재검증하고 409로 거부한다. 대부분의 쇼핑몰은 예정이라 안 막지만, 보장을 팔면 이 거부가 필요하다.

**결제 전까지 보장일은 계약이 아니다**
그래서 재시도 시 낡은 값을 갱신한다. 저장값과 대조하면 마감을 넘긴 주문은 영영 결제할 수 없다. 검증 대상이 금액(저장값)과 보장일(지금 계산값)로 다른 이유가 이것.

**5분 룰 — 생성 후 5분 안에 확정, 넘기면 취소**
5분은 임의 값이 아니라 승인 호출의 HTTP 타임아웃(연결 10초 + 읽기 60초 = 최대 70초)에서 유도된다. 취소를 쏠 때 날아다니는 승인 요청이 없다는 것이 보장된다. 업계 표준(망취소)보다 늦게 쏘는 이유는, Toss 취소 API가 승인 응답에만 있는 `tid`를 요구해 **선제적으로 막을 수단이 없기** 때문이다. 조회로 알아내야 하므로 기다렸다 판단할 수밖에 없다.

**404 판정 — 망취소 쪽으로 보내고, 두 번째 조회에도 없으면 종료** *(2026-08-17 결론)*
조회의 404는 "영원히 없음"(요청이 도달한 적 없음)과 "아직 없음"(떠돌다 곧 도착)을 구분하지 못한다. 처리 스케줄러가 판단하지 않고 망취소 경로로 넘기면, 그쪽이 어차피 조회부터 하므로 **시간이 지나며 저절로 판별된다.** 그때도 404면 실제로 없는 것으로 보고 종료.

**마감 배치는 PAID만 조회한다 — 그래서 스케줄러 간 순서 조율이 필요 없다**
미확정 주문은 애초에 대상이 아니고, 5분 룰이 곧 취소로 보낸다. 확인 스케줄러가 늦게 돌면 그 주문은 오늘 못 나가고 취소될 뿐이며 그것이 규칙상 맞는 결과다. 두 잡을 한 잡으로 합치거나 실행 순서를 맞출 이유가 없다. **처음엔 "마감 스윕 → 전달"을 한 잡에 순차로 넣으려 했으나, 문제 자체를 없애는 쪽이 나았다.**

**"망취소 대기" 상태를 만들지 않는다**
`created_at`이 이미 그 정보를 갖는다("PENDING인데 5분 지남"). 상태를 추가하면 같은 사실을 두 곳에서 말하게 된다. 취소 실패 재시도도 PENDING으로 두면 다음 회차에 자동으로 걸린다. 취소 트리거가 둘 이상이 되면(예: 보장 위반) 그때 추가.

**이미 취소된 결제에 취소를 반복하지 않는다**
조회가 매 회차 취소보다 먼저 돌고, 이미 취소된 결제는 `CANCELED` → `Failure`로 끝나 취소 경로에 도달하지 않는다. 별도 처리가 필요 없다.

**회차 밀림 — 대책을 넣지 않고 일일 대사로 받는다** *(2026-08-22 결론)*
회차를 미는 70초짜리 조회는 응답이 아니라 타임아웃이고, 결과가 `Unknown`이라 성과가 0이다. 즉 밀림이 일어나는 구간은 Toss 장애 하나뿐이며, 그때는 병렬로 돌려도 전부 Unknown이라 얻는 것이 없다. 미확정이 여러 건 쌓이는 조건도 Toss 장애나 우리 DB 장애뿐이고 평상시에는 한두 건씩 흩어져 들어온다. **PENDING이 많아서 Toss가 느려지는 되먹임은 없다** — 길어지는 것은 우리 회차뿐이다(느려지는 쪽의 되먹임은 동기 승인의 스레드 점유이고, 스케줄러와 무관하다).
그래서 병렬 조회·서킷 브레이커·회차 시간 예산을 모두 넣지 않는다. 밀림의 실제 피해는 "눈감고 Unknown을 종료한다"이고, 그것을 받는 것은 일일 대사다.
서킷 브레이커가 공짜가 아닌 이유: 회차를 건너뛰면 Toss 복구 후 진짜 답을 보고 판단할 수 있지만 그동안 사용자는 PENDING으로 붙잡힌다. 배송 보장 상품은 어차피 오늘 못 나가므로, 빨리 실패를 알려 재결제하게 하는 편이 낫다. 돈 환불이 늦는 것은 대사가 있으면 정합성 문제가 아니다 — 다만 2026-08-23부터 대사는 취소하지 않고 적재만 하므로, 환불은 사람이 처리할 때까지 늦는다.

**대사가 싼 이유 — 고아 거래가 없다** *(2026-08-22 결론)*
`BillingPaymentFacade`에서 `prepare()` 커밋이 승인 호출보다 앞이라, Toss에 존재하는 거래는 반드시 우리 DB에 대응 행이 있다. 우리 DB가 PG의 상위집합이라 **고아 거래(PG엔 있고 우리엔 없음)는 원리상 나오지 않는다.** 그래도 구현은 PG 일일 내역을 통째로 긁는다 — 전수 대조가 그 전제를 검증하는 축이고, `ORPHAN`이 뜨면 전제가 깨진 것이다. 대사가 싼 이유는 내역을 안 긁어서가 아니라 **판정이 표 하나로 끝나서**다. (2026-08-23: 처음엔 여기에 취소까지 넣었다가 아래 «대사는 적재만 한다»에서 걷어냈다.)

**대사는 적재만 한다 — 처분은 재처리 한 곳으로** *(2026-08-23 결론)*
대사가 하던 일이 셋이었다 — 대조하고, 취소하고, 어제 미결을 종결했다. 특히 **미결을 언제 끊을지는 대사가 정할 일이 아니다.** `netCancel`이 취소 실패를 무한 재시도로 남기고 그 끊는 지점을 대사가 대신 집행해서, `closeStuckPending`이 재처리의 결정표(조회 → 확정 / 취소 / 포기)를 통째로 복제하고 있었다. 두 잡이 한 규칙을 나눠 가져 어느 쪽 주석도 혼자서는 참이 아니었다.
그래서 대사에서 취소를 전부 걷어냈다(−88줄, 추가는 enum 상수 둘). Toss 호출이 세 곳(`netCancel`·`cancelLate`·`closeStuckPending`)에서 **재처리 한 곳**으로 줄고, 대사는 `transactions` 한 번만 읽는다. 성공 건의 개별 `inquiry` 재확인도 없앴다 — 우리 `approved_at`도 Toss가 준 값이라 두 목록의 축이 이미 같고, 어긋난다면 그게 곧 사람이 봐야 할 불일치다.
업계 관행도 같은 방향이다. 대사는 적재하고 사람에게 넘기며, 자동 처분은 실시간 경로가 한다. 대사가 처분하지 않는 주된 이유(정산 소스가 T+1~T+3에 확정돼 불일치 다수가 타이밍 차이라 자동 처분하면 정상 거래를 건드린다)는 **우리에게 약하게 적용된다** — 우리 소스는 정산 파일이 아니라 실시간 API 조회다. 그래도 층위를 섞지 않는다는 원칙은 그대로 둔다.
처분 전용 잡은 만들지 않았다. 불일치는 적고 사람이 본다. `ReconciliationDiscrepancyRepository`는 insert 전용으로 남고, 종결(OPEN → RESOLVED)은 관리자가 한다.

**받아들인 대가 — 셋 다 의도한 것이다.**
- **자동 환불이 없다.** "우리는 실패인데 돈은 나갔다"(`PAID_BUT_FAILED`)의 해소가 사람 손에 달렸다. 지금 창구는 H2 콘솔뿐이다. 걷어낸 `cancelLate`의 주석이 그걸 "이 잡의 존재 이유"라 불렀으니 리팩터가 아니라 **동작 변경**이다.
- **미결이 자동으로 끝나지 않는다.** 취소가 `NOT_CANCELABLE_*` 등으로 계속 실패하는 거래는 PENDING으로 남고 재처리가 30초마다 계속 본다. "어제 거래는 종착 상태만 남는다"는 보장이 없어졌고, 재처리 조회 대상이 하루로 잘리지 않아 **회차 밀림이 상한 없이 누적된다**(이전엔 대사가 하루로 잘랐다). 대신 생성 다음날 대사가 `STUCK_PENDING`으로 **한 번** 적재하고, 그 행이 `OPEN`으로 남아 관리자 큐에 계속 보인다. **매일 재적재하지 않는 것이 의도다** — 불일치 행은 일회성 알림이 아니라 미결 티켓이라(`create()`는 항상 OPEN, 리포지토리는 insert 전용, 종결은 관리자만) 행은 사라지지 않는다. 같은 사실을 매일 다시 넣으면 한 문제로 행이 N개 쌓여 큐가 오염될 뿐이다. 배치가 매일 도는 것은 **새 불일치를 찾기 위해서**고, 이미 찾은 것의 처리는 관리자가 큐를 보고 한다.
- **그 주문은 재결제가 막힌 채 남는다.** `prepare()`가 PENDING 거래를 보고 `AlreadyPending`을 돌려주므로 이중 청구는 없다. 대신 사용자는 그 주문으로 다시 결제할 수 없다.

대사의 판정 어휘도 이에 맞춰 정리했다. `CANCEL_FAILED`(취소 실패 — 대사가 취소하지 않으므로 무의미)를 지우고, `PAID_BUT_FAILED`("우리는 실패인데 PG는 승인", `CANCELED_BUT_SUCCESS`와 대칭)와 `STUCK_PENDING`("종결되지 않은 미결")을 넣었다.
미결은 **두 곳에서** 적재한다 — PG 목록에 있으면 `reconcile()`이 `pg`와 함께, 없으면 `recordFromOurSide`가 `pg=null`로. 처음엔 한 곳으로 합쳤다가(ponytail 리뷰) 되돌렸다: **`pg_status`가 채워졌는지 여부가 곧 삼각분류 축이다.** 채워지면 "돈이 잡혀 있다"(고객은 청구됐고 화면은 대기 중 — 급하다), 비면 "승인이 도달조차 못 했다"(양성). 합치면 두 상황이 바이트 단위로 같은 행이 되어, 관리자의 첫 질문("돈이 나갔나")에 행이 답을 못 하고 record-only가 덜어주려던 건별 수동조회를 도로 시킨다. 게다가 이 값은 **나중에 복구되지 않는다** — 스키마가 `pg_status`를 "감지 시점 PG 판정"으로 정의하고 행을 박제로 두는데, 며칠 뒤 조회하면 그때의 상태가 나온다. 같은 유형을 두 곳에서 쓰지만 **서로 다른 사실**을 쓴다.
---

## 2. 안 하기로 한 것

이 프로젝트의 범위 밖으로 확정한 것들. 다시 꺼내지 않는다.

| | 내용 | 알고 넘어가는 것 |
|---|---|---|
| **인증** | companySeq가 요청 본문의 숫자다 | 남의 거래를 조회하는 것을 막을 수 없다. 키에 companySeq를 섞는 코드 TODO도 인증이 전제라 같이 보류된다 |
| **영업일 달력** | `plusDays(1)`이 일요일·공휴일·물류사 휴무를 넘어간다 | **토요일 주문이 일요일 도착을 보장한다.** 실제 보장 서비스는 전부 배송 가능일 달력에 대고 계산한다 |
| **재고** | 물건이 없으면 못 보낸다 | 보장의 나머지 절반이다. 지금 보장은 "결제가 제때 확정되었다"까지만 뜻한다 |
| **지역** | 도서산간 제외/+1일 | 같은 이유 |

---

## 3. 미뤄둔 것

코드 TODO에 없는 것들. **여기 안 적으면 잊힌다.**

| | 내용 | 왜 미뤘나 |
|---|---|---|
| **인증 결제** | 결제창에서 인증(본인인증·3DS)을 거친 뒤 서버가 승인하는 2단계 결제. 지금은 빌링키 전용이라 인증이 발급 시점에 끝나 있고, 결제 요청이 도달하는 순간 곧바로 승인이다 | **할 예정**(2026-08-22). 인증과 승인 사이에 **사용자가 브라우저를 떠날 수 있는 구간**이 생기는 것이 핵심 차이다. `returnUrl` 복귀, 인증은 됐는데 승인이 실패한 경우, 복귀하지 않은 거래의 처분이 새로 붙는다. 지금의 재처리 스케줄러와 결과 페이지 폴링을 그대로 쓸 수 있는지가 첫 질문 |
| **보장 확인 축** | 약속한 날짜에 실제로 실렸는지 대조 | 없음. 위반이 조용히 지나간다. 보장 상품이면 클레임 대응·보상 근거가 되는 축 |
| **취소된 사용자 안내** | 미확정으로 취소된 사람에게 "다시 결제하면 8/18 배송" 같은 안내 없음 | 결과 페이지가 FAILED와 사유만 보여줌 |
| **`AlreadyPending` 안내** | 재시도했는데 옛 거래가 확정되면, 사용자는 새로 결제한 줄 앎 | 안내 없이 결과 페이지로 보냄 |
| **폴링 종료 후 경로** | 2분 폴링이 미확정으로 끝나면 URL 말고 확인할 방법 없음 | 주문내역 화면 부재 |
| **클라이언트 폴링과 서버 확인의 시간 차** | 승인이 `Unknown`으로 끝나면 서버는 생성 후 90초가 지나야 첫 조회를 한다. 클라이언트는 2·4·8·15초로 두드리므로 그때까지는 같은 `PENDING`만 받는다. 답은 95~105초쯤 뜨고 폴링 창(120초) 안이지만, 5초에 알 수 있는 것을 95초에 안다 | 승인 종료를 표시하면(`approve_returned` 같은 불린) 90초를 면제할 수 있다. 다만 90초는 "승인 중인 거래는 조회하지 마라"로도 작동해서, 면제하면 Toss가 느릴 때 조회가 늘어 회차 밀림을 악화시킨다. 한때 `next_check_at` 컬럼과 `checkNow()`로 이걸 당겼는데, 그 컬럼이 파생 값이라 혼동을 세 번 만들어 제거했다 |
| **웹훅** | Toss가 상태 변화를 우리 엔드포인트로 push(동기·폴링의 pull과 반대 방향). 가상계좌·구독 계열에선 필수라 표준이지만, 카드+빌링키 동기 승인에선 보강 채널이지 필수가 아니다 — HAR로 관찰한 무신사·쿠팡·네이버도 겉으로는 동기+폴링 구조 | 다음에 다시 논의(2026-08-22). 로컬이라 수신 자체가 불가한 것이 1차 이유. 사주는 것은 "Unknown 확정 95초 → 몇 초"(위 폴링 시간 차 해소)뿐이고 **정합성엔 기여 없음** — 대사가 이미 막는다. 대가는 크다: 웹훅은 모든 결제에 오므로 동기 응답과 **상시 경합**이 생기고(웹훅이 먼저 쓰면 동기 쪽 CAS 0행 → 지금 코드는 500, 돈은 나갔는데 사용자는 실패를 봄), 해법은 "내가 썼는가"가 아니라 **"원하는 상태가 되었는가"로 성공 판정을 바꾸는 수렴 시맨틱** + 잠금 순서 통일(같은 전이 함수 재사용) + 유실·중복·순서 방어(웹훅은 at-least-once) + PG 재발송에 기대는 실패 가능 핸들러. 지금 pull 구조(폴링·재처리·대사)는 주도권이 우리에게 있어 이 기계 일습이 통째로 불필요하다. **진짜 도입 시점은 가상계좌를 붙일 때** — 그땐 선택이 아니라 필수가 된다 |
| **배치의 건별 실패 처리** (코드 TODO 있음) | 대사는 건별로 try-catch 격리하고 로그만 남긴 뒤 다음 건으로 간다. 재처리에서는 안전한 패턴이었다 — 30초 뒤 다음 회차가 같은 거래를 다시 집으니까. 대사는 창이 1일이고 결석 만회도 없어, **한 건이 예외로 빠지면 그 거래는 영영 대사를 못 받는다.** 마지막 방어선에 구멍이 뚫린다 | 고민 중(2026-08-22). 지금 코드는 Spring Batch 가정과도 어긋난다 — "결석 만회는 인프라가 한다"고 정해놓고 실패를 전부 삼켜서 잡은 항상 성공으로 끝나므로, 인프라가 만회할 신호 자체를 안 준다. 유력안은 **격리는 유지하되 실패 건수를 세어 마지막에 잡을 실패시키는 것**(JobExecution이 FAILED가 되어 그 날짜로 재실행이 가능해진다). 재실행 안전성은 확인했다 — 정합 쌍은 no-op, 이미 취소한 건은 우리가 FAILED라 어느 분기도 안 타고, 적재는 애초에 중복을 허용한다. 대안으로 실패 자체를 불일치로 적재하는 방법이 있으나 "처리 실패"를 "불일치 유형"에 섞게 된다. **유력안에 빠진 전제(2026-08-23): 창을 날짜로 받아야 한다.** 지금 `reconcileYesterday()`는 창을 `Dates.today()`에서 만들어서, "그 날짜로 재실행"이 같은 날에만 뜻이 있다 — 이틀 뒤에 돌리면 그때의 어제를 대사하고 놓친 날짜는 영영 못 돌린다. 참고로 `recordFromOurSide`는 건별 격리가 없어 예외를 그대로 올려보내는데, 그게 노트가 없다고 지적한 "만회 신호"를 실제로 주는 쪽이다. 비대칭을 맞출 방향은 이쪽에 격리를 넣는 게 아니라 `reconcile`의 삼키기를 걷는 것이다 |
| **승인 호출에 총시간 상한이 없다** | `SimpleClientHttpRequestFactory`의 읽기 60초는 **읽기 사이 간격**(SO_TIMEOUT)이라 호출 총시간 상한이 아니다. Toss가 바이트를 조금씩 흘리면 승인이 70초를 넘고, 그러면 90초 가드를 뚫어 재처리가 승인 중인 거래를 집는다(CAS 0행 → 500) | 상한을 두려면 `JdkClientHttpRequestFactory` + `HttpRequest.timeout`으로 바꿔야 하는데 발급·승인·조회·취소에 다 걸린다. 확률이 낮아 90초를 확률적 가정으로 두고 미뤘다 |
| **mark가 컬럼을 다 덮는다** | mark는 tid·approved_at·fail_code·fail_message를 무조건 SET하므로 넘기지 않은 값은 NULL로 지워진다 | 지금은 휴면(2026-08-23) — SUCCESS→FAILED 전이가 없어 지워질 값이 있는 경로가 없다. 그런 전이가 생기면 대사가 창을 자르는 approved_at을 지우게 되므로 그때 부분 UPDATE로 바꾼다 |
| **우리 DB 장애로 성공한 결제가 취소된다** | `prepare()` 커밋 → 승인 성공(돈 나감) → `complete()` 쓰기 실패면 거래가 PENDING으로 남는다. DB가 5분 넘게 죽어 있으면 회복 후 이미 기한을 넘긴 상태라, 재처리가 `DONE`을 확인하고 **취소를 쏜다** | 5분 기한은 "22:05 배치 전에 결론이 나야 한다"의 대리값인데, 오전 10시 장애라면 배치까지 12시간이 남아 마감 압박이 없다. 규칙만 보고 취소하는 셈. 5분 룰을 정할 때 "Toss 탓"을 전제했는데 이건 **우리 탓**이라 성격이 다르다. 기한을 절대 시각(마감)에 연동할지, 장애 구간을 제외할지 등 미정. **다만 우선순위는 낮다(2026-08-22).** 최악이 "안 취소해도 됐을 것을 취소함"이라 돈은 돌아가고 정합성은 맞는다. 오유실(돈 나갔는데 우리는 모름)과 **반대 방향의 실수**다 |
| **창을 걸친 거래가 허위 불일치를 만든다** (`PAID_BUT_FAILED`·`MISSING_AT_PG`) | 23:57 생성 → 23:58 승인(D-1 거래) → 응답 유실 → 00:02 망취소(D 거래). 00:30 대사는 `[D-1, D)`만 긁어 PAID 행만 보고, 우리는 `NET_CANCEL`/FAILED라 **이미 환불된 결제**를 최고 등급으로 적재한다. 노출 구간은 `23:55~00:00 생성 + 미확정 + 망취소`. D+1 대사에는 CANCELED 행이 들어와 정합 처리되니 허위는 딱 한 번이다 | **사실 확인이 먼저다(2026-08-23).** 문서로 확인된 것: 거래 조회는 `transactionAt`("거래가 처리된 시점") 기준으로 자르고, `transactionKey`는 "한 결제 건의 승인 거래와 취소 거래를 구분"하며, 전체 취소도 별도 거래다. **안 갈린 것: `status`의 단위.** 문서가 "**결제** 처리 상태"라고 써서, 각 행이 그 거래의 결과가 아니라 결제의 현재 상태를 실어 보내면 D-1 승인 행도 취소 후 `CANCELED`로 보이고 **이 문제 자체가 없다.** 우리 fold가 행끼리 불일치를 전제하지만 그건 확인 없이 쓴 코드라 근거가 못 된다. 테스트 키로 승인·취소 후 `/v1/transactions`를 긁어 행 수와 각 행의 `status`·`transactionAt`을 보면 5분에 갈린다. 수선안 셋 — **(A)** `failCode`가 취소를 증거하면(`NET_CANCEL`·`PG_CANCELED`) 건너뛴다(4줄, 그러나 감사 대상인 우리 기록으로 경보를 끄는 자기모순). **(B)** PG 목록만 `[D-1, D+1)`로 넓혀 걷고 판정 대상은 그대로(근본 수선이나 "긁는 창"과 "판정하는 창"이 갈리고, 모레 취소면 또 뚫림). **(C)** 이 분기에서 `inquiry` 한 번으로 현재 상태 확인 후 적재(사각지대 없음, 후보만 통과하니 평상시 0건, 대신 "대사는 한 번만 호출"이 깨짐). **안 하는 것도 선택지다** — 돈은 안 움직이고 관리자가 조회 한 번 하면 밝혀진다. **같은 뿌리의 두 번째 증상: `MISSING_AT_PG`(2026-08-23).** 대사는 우리 쪽을 `approved_at`(= Payment.`approvedAt`, "결제 승인이 일어난" 시각, **결제 단위·하나·불변**)으로 자르고 PG 쪽은 `transactionAt`("거래가 처리된" 시각, **거래 단위·여럿**)으로 자른다 — 문서가 다른 두 필드고, 승인 거래에서 둘이 같은 값이라는 서술은 없다. 23:59:59 승인이 한쪽에선 D-1, 다른 쪽에선 D로 떨어지면 건강한 결제가 최고 위험 등급으로 적재된다. 걷어낸 `auditOurSuccesses`의 주석이 정확히 이 케이스를 지목했고("PG의 거래 시각과 미세하게 어긋날 수 있어 개별 조회로 한 번 더 확인한다"), 없앨 때 든 근거 "두 축이 같다"는 확인 없는 추정이었다. 여기서는 (A)(B)가 안 통한다 — 우리 쪽에 "PG에 있다"를 증거할 값이 없어 **(C) 재확인만 실질적**이다. 규모도 다르다: `PAID_BUT_FAILED`는 후보가 평상시 0건이라 호출이 안 나가지만 이쪽은 경계에 걸린 건수만큼 나간다. **둘은 같은 사실 하나에 달려 있어 테스트 한 번으로 함께 갈린다** — 승인 후 `/v1/transactions`를 긁어 승인 행의 `transactionAt`이 응답의 `approvedAt`과 같은지, 취소 후 행이 몇 개이고 각 `status`가 무엇인지 보면 된다 |
| **취소 실패 사유가 로그에만 남는다** | `netCancel`이 취소에 실패하면 로그에 reason을 찍는 것이 전부다. 이 PR 전에는 대사의 `CANCEL_FAILED` 불일치 행이 `detail`에 영속 보관했고, `3f3cd30`이 그걸 **"관리자가 사유를 알아야 하는 유일한 자리"**라 부르며 일부러 확보한 것이었다. 대사에서 취소를 걷어내며 그 자리가 사라졌다 | **붙일 자리를 못 찾았다(2026-08-23).** `fail_message`는 취소 실패 시 FAILED로 바꾸지 않으니 쓸 시점이 없고(`mark`는 PENDING→FAILED 전이만 한다), 불일치로 적재하면 재처리가 30초마다 도니 같은 사유로 행이 무한 증식하며(중복 검사가 없다), 다음날 대사는 취소 오류를 몰라 `STUCK_PENDING.detail`에 넣을 수 없다. 로그는 로테이션되고 불일치 큐를 보는 관리자는 로그를 안 본다 — **영속 보관은 복구되지 않았다.** 미결에 쓸 수 있는 칸(마지막 취소 오류)을 하나 두면 풀리지만 컬럼 추가만큼 값어치가 있는지는 미정 |
| **갇힌 미결이 영구히 Toss 호출을 태운다** | 취소가 `NOT_CANCELABLE_*` 등으로 계속 실패하는 거래는 PENDING으로 남고 재처리가 30초마다 계속 집는다. 건당 `inquiry`+`cancel` 2회이므로 **하루 5,760회**, 성공 가능성은 0이고 개체수는 단조 증가한다. 이 PR 전에는 대사가 어제 생성분을 종결해 하루로 잘렸다 | **선두 점유는 고쳤다(2026-08-23)** — `findPendingDueForCheck`를 `ORDER BY created_at DESC`로 바꿔 새 거래가 먼저 처리된다. 회차는 전량을 돌므로 커버리지는 같고, 판정은 건별 `Instant.now()`라 순서에 영향받지 않는다. 남은 것은 트래픽인데 고칠 두 방법이 **둘 다 이미 거부된 것**이다 — 나이 상한으로 제외하면 뒷문으로 들어온 포기 기준이라 "끊고말고는 대사가 정하지 않는다"에 반하고, `next_check_at` 백오프 컬럼은 Step 6에서 파생 값이라 혼동을 세 번 만들어 없앤 그 컬럼이다. Toss 레이트 리밋에 걸리면 그때 다시 본다 |

### 코드에 박힌 TODO — 6개

`grep -rn "TODO" src/main`으로 확인한다. 상세는 코드 줄에 그대로 두고 여기엔 요지와 조건만 둔다 — **붙일 코드 줄이 있는 것은 코드에 남긴다**는 이 문서의 규칙 그대로다. 이 표는 색인이다.

| 위치 | 내용 | 언제 / 조건 |
|---|---|---|
| `BillingPaymentFacade` 승인 호출 앞 | **B안(비동기) 전환.** 요청 스레드가 Toss 승인을 동기로 기다린다. 전환 이유가 둘이고 성격이 다르다 — (1) 부하: 스레드 풀 고갈, 지금 규모는 아니다. (2) **가용성: Toss가 죽으면 스레드가 물려 주문 접수 자체가 막힌다** | (2)는 규모와 무관한 장애 격리 문제라 "규모가 오면"으로 미룰 성격이 아니다. 전환은 네이버식 — 접수(`prepare`)까지만 하고 즉시 응답, 승인은 워커가 비동기 처리. 결과 페이지·지수 백오프 폴링·재처리 스케줄러는 그대로 재사용 가능 |
| `BillingPaymentFacade` Success 분기 | **승인 후 DB 실패가 500으로 나간다.** `complete()`가 실패하면 "다시 시도해 주세요"가 뜨는데 돈은 이미 빠져나갔다. 승인 **전** 실패(돈 안 나감 → 재시도가 맞다)와 승인 **후** 실패(돈 나감 → 확인으로 보내야 한다)를 구분해야 한다. 받아둔 tid도 롤백과 함께 버려진다 | 불확실한 `Unknown`은 폴링시키면서 성공을 확인한 이 경로를 더 나쁘게 다루는 셈이다. `Unknown`처럼 PENDING 응답으로 결과 페이지에 보내는 편이 맞다 |
| `PaymentWorkflowService.complete` 앞 | **보장 위반 처분이 미정.** 재처리가 PENDING을 뒤늦게 확정하면 주문의 배송 보장일이 이미 못 지킬 값일 수 있다. 확정 시점 재계산으로 **감지**까지는 필요해 보이는데 그 뒤가 안 정해졌다 — 취소(못 지킬 보장은 팔지 않는다) vs 다음 회차로 미루고 보상(쿠팡식. 약속일과 실제 태울 날짜를 **둘 다** 저장해야 약속이 남아 보상 근거가 된다) | 어느 쪽이든 취소 API나 스키마 변경이 따르므로 스케줄러 설계와 같이 정한다 |
| `BillingPaymentCommand` 키 생성 | **키에 companySeq를 섞어 회사별 네임스페이스를 만든다.** `order_key`·`payment_key`를 `"${companySeq}-${클라이언트 키}"`로. 파생 키가 전역 유일하면서 회사별로 갈라져 스키마와 조회 시그니처는 그대로 두면 되고, moid는 `payment_key`라 손댈 것이 없다 | **인증이 먼저다.** 인증 전에 넣으면 승인 응답을 못 받은 클라이언트에는 원본 키뿐이라 결과 페이지가 404가 된다. 피하려면 클라이언트가 파생 규칙을 복제해야 하는데, 그건 인증이 붙으면 지워야 할 코드다 |
| `OrderService.findByDeliveryDateAndStatus` | **마감 배치 대상을 전체 한 번에 읽는다.** 주문이 많아지면 id 기준 seek 페이징으로 나눈다. **offset은 쓸 수 없다** — 처리하면서 상태가 바뀌어 대상에서 빠지므로 건너뛰는 건이 생긴다 | 주문량이 커지면 |
| `DailyReconciliationService.reconcileYesterday` | **대사의 건별 실패 처리.** 위 표의 「배치의 건별 실패 처리」와 같은 항목이다 | 고민 중. 유력안과 그에 빠진 전제(창을 날짜로 받아야 한다)는 위 항목에 적어뒀다 |

회차 밀림은 Step 6에서 드러났다. 조회가 순차라 Toss가 느리면 회차가 길어지고, 그러면 "생성 후 5분에
확정한다"가 지켜지지 않는다. 같이 드러난 "첫 만료가 승인 타임아웃보다 짧음"은 이 브랜치에서 고쳤다
(첫 확인 대기와 재조회 간격을 분리했고, 이후 next_check_at 컬럼 자체를 없애 created_at으로 대체했다).

### 회차 밀림 — 이어서 볼 때 참고 *(2026-08-20 분석 · 2026-08-22 종결)*

거래 하나가 조회 대상인 구간은 생성 후 90초~300초, 즉 **210초**다. 이 안에 최소 한 번 조회돼야 5분 룰이
지켜진다. 한 회차는 `미확정 건수 × 조회 시간`(순차)이고, `fixedDelay`라 다음 회차는 이전이 끝난 뒤 30초부터 센다.

| 동시 미확정 | 조회 70초 기준 회차 | 한 거래가 조회되는 주기 | 210초 안에 조회 |
|---|---|---|---|
| 1건 | 70초 | 100초 | 2번 |
| 3건 | 210초 | 240초 | 0~1번 |
| 5건 | 350초 | 380초 | **0번** |

**3건만 돼도 무너진다.** 그리고 미확정이 여러 건 쌓이는 상황은 Toss가 느릴 때뿐인데, 하필 그때 조회도
70초씩 걸린다 — 문제가 터지는 조건과 처리가 느려지는 조건이 같다.

우리 DB 장애로 쌓이는 경우는 계산이 다르다. Toss는 멀쩡하니 조회가 1~2초라 210초에 100건쯤 처리된다.
그쪽은 밀림보다 위의 "성공한 결제가 취소된다"가 더 큰 문제다.

| 대책 | 하는 일 | 처리량 |
|---|---|---|
| 회차 시간 예산 | 회차를 X초에 끊고 나머지는 다음으로 | 안 늘어남 — 회차 길이만 예측 가능. 뒤쪽 거래는 계속 굶는다 |
| 마감 임박 우선 정렬 | 기한 가까운 것부터 | 안 늘어남 — 급한 것만 챙긴다 |
| 병렬 조회 | 동시에 K개 | **늘어남** — 회차가 합에서 최댓값으로 |
| 서킷 브레이커 | 조회가 연속 실패하면 회차를 건너뜀 | Toss 장애 때 헛수고를 안 함 |

조회 타임아웃 축소는 선택지가 아니다 — Toss가 최소 60초를 권장한다.

**결론: 넷 다 넣지 않는다.** 근거는 1장 «회차 밀림 — 대책을 넣지 않고 일일 대사로 받는다».

---

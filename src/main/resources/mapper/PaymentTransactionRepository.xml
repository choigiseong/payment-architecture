<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coco.payment.persistence.repository.PaymentTransactionRepository">
    <sql id="paymentTransactionColumns">
        pt.id AS id,
        pt.payment_key AS paymentKey,
        pt.order_seq AS orderSeq,
        pt.moid AS moid,
        pt.tid AS tid,
        pt.amount AS amount,
        pt.status AS status,
        pt.approved_at AS approvedAt,
        pt.fail_code AS failCode,
        pt.fail_message AS failMessage,
        pt.created_at AS createdAt,
        pt.updated_at AS updatedAt
    </sql>

    <insert id="insert" parameterType="com.coco.payment.persistence.model.PaymentTransaction"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment_transaction (payment_key, order_seq, moid, tid, amount, status)
        VALUES (#{paymentKey}, #{orderSeq}, #{moid}, #{tid}, #{amount}, #{status})
    </insert>

    <select id="findById" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.id = #{id}
    </select>

    <select id="findByPaymentKey" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.payment_key = #{paymentKey}
    </select>

    <select id="findByOrderSeqAndStatus" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.order_seq = #{orderSeq}
          AND pt.status = #{status}
    </select>

    <select id="findPendingDueForCheck" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.status = #{status}
          AND pt.created_at &lt;= #{approveDoneBefore}
        ORDER BY pt.created_at
    </select>

    <select id="findByMoid" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.moid = #{moid}
    </select>

    <select id="findByStatusAndApprovedAtBetween" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.status = #{status}
          AND pt.approved_at &gt;= #{from} AND pt.approved_at &lt; #{to}
    </select>

    <select id="findByStatusAndCreatedAtBefore" resultType="payment_transaction">
        SELECT <include refid="paymentTransactionColumns" />
        FROM payment_transaction AS pt
        WHERE pt.status = #{status}
          AND pt.created_at &lt; #{before}
        ORDER BY pt.created_at
    </select>

    <update id="mark">
        UPDATE payment_transaction
        SET tid = #{tid},
            status = #{toStatus},
            approved_at = #{approvedAt},
            fail_code = #{failCode},
            fail_message = #{failMessage}
        WHERE id = #{id}
          AND status = #{fromStatus}
    </update>
</mapper>

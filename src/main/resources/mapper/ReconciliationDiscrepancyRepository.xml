<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coco.payment.persistence.repository.ReconciliationDiscrepancyRepository">
    <insert id="insert" parameterType="com.coco.payment.persistence.model.ReconciliationDiscrepancy"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reconciliation_discrepancy (payment_system, type, status, moid, our_status, pg_status, our_amount, pg_amount, detail)
        VALUES (#{paymentSystem}, #{type}, #{status}, #{moid}, #{ourStatus}, #{pgStatus}, #{ourAmount}, #{pgAmount}, #{detail})
    </insert>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coco.payment.persistence.repository.OrderItemRepository">
    <sql id="orderItemColumns">
        oi.id AS id,
        oi.order_seq AS orderSeq,
        oi.item_name AS itemName,
        oi.unit_price AS unitPrice,
        oi.quantity AS quantity,
        oi.created_at AS createdAt,
        oi.updated_at AS updatedAt
    </sql>

    <insert id="insert" parameterType="com.coco.payment.persistence.model.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_seq, item_name, unit_price, quantity)
        VALUES (#{orderSeq}, #{itemName}, #{unitPrice}, #{quantity})
    </insert>

    <select id="findById" resultType="order_item">
        SELECT <include refid="orderItemColumns" />
        FROM order_item AS oi
        WHERE oi.id = #{id}
    </select>

    <select id="findByOrderSeq" resultType="order_item">
        SELECT <include refid="orderItemColumns" />
        FROM order_item AS oi
        WHERE oi.order_seq = #{orderSeq}
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coco.payment.persistence.repository.CompanyBillingKeyRepository">
    <sql id="companyBillingKeyColumns">
        cbk.id AS id,
        cbk.company_seq AS companySeq,
        cbk.payment_system AS paymentSystem,
        cbk.customer_key AS customerKey,
        cbk.billing_key AS billingKey,
        cbk.created_at AS createdAt,
        cbk.updated_at AS updatedAt
    </sql>

    <insert id="insert" parameterType="company_billing_key"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO company_billing_key (
            company_seq,
            payment_system,
            customer_key,
            billing_key
        ) VALUES (
            #{companySeq},
            #{paymentSystem},
            #{customerKey},
            #{billingKey}
        )
    </insert>

    <select id="findByCompanySeqAndPaymentSystem" resultType="company_billing_key">
        SELECT <include refid="companyBillingKeyColumns" />
        FROM company_billing_key AS cbk
        WHERE cbk.company_seq = #{companySeq}
          AND cbk.payment_system = #{paymentSystem}
    </select>

    <update id="updateBillingKey">
        UPDATE company_billing_key
        SET billing_key = #{billingKey}
        WHERE id = #{id}
    </update>
</mapper>
